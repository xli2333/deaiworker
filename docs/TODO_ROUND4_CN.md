# de_ai_worker 原子级 Todo Round 4

更新时间：`2026-04-17`

## 本轮目标

1. 解决结构化 JSON 偶发损坏导致的 `Unterminated string in JSON...`。
2. 在任务面板中持续显示当前步骤、最近一次审查意见、具体问题与修订建议。
3. 完成构建与烟测，确认没有新的中文编码或 JSX 回归。

## 执行规则

1. 做完一项勾一项。
2. 本轮只解决当前明确问题，不扩散到无关重构。
3. 本轮结束后复核是否还有明确提升空间；如果有，再开 Round 5。

## 当前进度

- [x] 建立本轮中文 Todo
- [x] 增强模型 JSON 解析鲁棒性
- [x] 为解析失败提供修复与保守回退
- [x] 在后端任务状态中暴露当前审查意见与问题列表
- [x] 在前端任务面板中显示当前步骤、摘要、问题与错误细节
- [x] 完成本轮构建验证
- [x] 完成本轮烟测
- [x] 复核是否仍需 Round 5

## 原子级清单

### A. 结构化输出稳态化

- [x] A1. 为模型 JSON 输出增加多轮本地解析尝试
- [x] A2. 解析失败时接入 repair/fallback 兜底
- [x] A3. 避免把 `Unterminated string in JSON...` 直接暴露成致命错误

### B. 后端状态增强

- [x] B1. 在任务状态中增加 `reviewState`
- [x] B2. 在任务历史中写入审查摘要、问题和修订建议
- [x] B3. 在失败状态中保留更清晰的阶段上下文和错误细节

### C. 前端显示增强

- [x] C1. 显示当前所在步骤
- [x] C2. 显示最近一次审查摘要
- [x] C3. 显示问题列表、判断、修订建议与片段
- [x] C4. 在日志中显示详细上下文

### D. 验收

- [x] D1. `npm run build`
- [x] D2. 运行烟测脚本
- [x] D3. 复核是否仍有明确提升空间

## Round 4 结论

接口级验收已经确认：

1. 结构化审查在 JSON 损坏时会改走修复/回退，不再直接因为 `Unterminated string in JSON...` 中断。
2. 任务状态中已经能看到当前步骤、最近一次审查摘要、问题列表和详细日志。
3. 真实验收仍发现本地 mock 链路会残留悬空连接句，因此已按规则进入 `Round 5` 继续收紧。
