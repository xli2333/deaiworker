# de_ai_worker 原子级 Todo Round 9

更新时间：`2026-04-17`

## 本轮目标

1. 继续修复真实链路里仍存在的反馈跑偏问题，重点处理报告字段中的重复废话和残留异常文本。
2. 给真实模型生成后的正文补上一层确定性的本地收口，清理中英/中数多余空格等稳定问题。
3. 再次用 `23andMe` 真实文稿回归，确认反馈更干净、终稿更稳。

## 当前进度

- [x] 建立本轮中文 Todo
- [x] 压缩报告字段中的重复废话和异常长文本
- [x] 统一 severity 归一化
- [x] 为真实模型输出补充本地确定性排版收口
- [x] 完成本轮构建验证
- [x] 完成真实链路回归验收
- [x] 复核是否仍需 Round 10

## 原子级清单

### A. 报告清洗

- [x] A1. 清理 repeated phrase / runaway text
- [x] A2. 限制 summary / toneGuardrail / unresolvedRisk 的失控长度
- [x] A3. 归一化 severity 值，避免中英文混杂

### B. 正文收口

- [x] B1. 对真实模型生成的中间稿补充中英/中数贴排清理
- [x] B2. 对终稿补充确定性排版收口

### C. 验收

- [x] C1. `node --check server/deaiEngine.mjs`
- [x] C2. `npm run build`
- [x] C3. 用真实 Key 再跑 `23andMe` 文稿
- [x] C4. 反馈文本更干净，终稿残留问题减少

## Round 9 结论

1. 报告字段里的重复废话和失控长文本已被压缩，`severity` 也统一归一化为 `high / medium / low`。
2. 真实模型生成后的中间稿和终稿现在会再走一层确定性的本地排版收口，重点清理中英/中数贴排问题。
3. 最新真实回归中：
   - 去AI初检与去AI复检都能给出具体、可对应正文片段的 `issues`。
   - 最终审计复检返回 `issues = 0`，判定文稿已达到可交付状态。
   - 标准烟测仍通过，没有引入新的流程回归。
4. 当前没有新的明确缺陷继续暴露，因此本轮结束后不再进入 `Round 10`。
