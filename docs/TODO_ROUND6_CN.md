# de_ai_worker 原子级 Todo Round 6

更新时间：`2026-04-17`

## 本轮目标

1. 继续收敛结构化 JSON 的截断类报错，重点处理 `Unterminated string in JSON ...`。
2. 在不依赖二次模型修复的前提下，先用本地启发式修复抢救被截断的 JSON。
3. 跑构建、烟测和损坏 JSON 回放，确认任务不会因这类输出直接失败。

## 当前进度

- [x] 建立本轮中文 Todo
- [x] 为本地 JSON 解析加入启发式修复
- [x] 支持字符串未闭合、字符串内裸换行、尾部缺失括号的抢救解析
- [x] 前端补充启发式修复状态文案
- [x] 完成本轮构建验证
- [x] 完成烟测
- [x] 完成损坏 JSON 回放验收
- [x] 复核是否仍需 Round 7

## 原子级清单

### A. 解析器增强

- [x] A1. 增加截断 JSON 的本地候选修复
- [x] A2. 增加字符串未闭合和裸换行的自动修复
- [x] A3. 增加基于检查点的安全截断回退

### B. 显示增强

- [x] B1. 为 `heuristic` 解析状态补充前端中文说明

### C. 验收

- [x] C1. `node --check server/deaiEngine.mjs`
- [x] C2. `npm run build`
- [x] C3. `npm run smoke`
- [x] C4. 用损坏 JSON 样例回放解析函数

## Round 6 结论

1. 本地解析器已经新增 `heuristic` 分支，会优先抢救被截断的 JSON。
2. 当前已覆盖三类高频损坏：字符串未闭合、字符串内裸换行、尾部缺少闭合括号。
3. 损坏 JSON 回放结果为 `ok: true`、`parserStatus: heuristic`，说明这类报错不再必然落到失败或保守回退。
4. 当前没有新的明确缺陷暴露，因此暂不进入 `Round 7`。
