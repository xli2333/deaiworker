# de_ai_worker 原子级 Todo Round 7

更新时间：`2026-04-17`

## 本轮目标

1. 去掉后端对正文输入的硬截断与硬上限，保证去AI、审计、终修都吃全量正文。
2. 去掉 JSON 修复链路对原始模型返回的截断，避免修复时再次丢失后半段。
3. 为文本输出增加自动续写拼接，避免模型第一次达到输出上限时直接留下半截稿。

## 当前进度

- [x] 建立本轮中文 Todo
- [x] 去掉正文输入的 `truncate(draft)`
- [x] 去掉 `18000` 字硬上限
- [x] 去掉 JSON 修复时对原始输出的截断
- [x] 为文本输出增加续写拼接
- [x] 完成本轮构建验证
- [x] 完成烟测
- [x] 完成长文无截断回归验收
- [x] 完成文本续写拼接回归验收
- [x] 复核是否仍需 Round 8

## 原子级清单

### A. 输入无截断

- [x] A1. 去AI初检吃全量正文
- [x] A2. 去AI修订吃全量正文
- [x] A3. 审计初检/修订/终修吃全量正文
- [x] A4. 去掉文稿长度硬限制

### B. 输出无静默截断

- [x] B1. JSON 修复链路吃全量原始返回
- [x] B2. 文本生成识别 `MAX_TOKENS`
- [x] B3. 文本生成自动续写并拼接去重
- [x] B4. 多轮续写仍未结束时显式报错，不静默交付半截稿

### C. 验收

- [x] C1. `node --check server/deaiEngine.mjs`
- [x] C2. `npm run build`
- [x] C3. `npm run smoke`
- [x] C4. 用超长 mock 文稿回放，确认不再因为 18000 字被拒
- [x] C5. 用 stub AI 回放 `MAX_TOKENS`，确认自动续写拼接

## Round 7 结论

1. 去AI初检、去AI修订、审计初检、审计修订、终修都已经改成吃全量正文，不再裁到 `18000` 字。
2. JSON 修复阶段也改成吃全量原始模型返回，不再把损坏 JSON 先裁到 `5000` 字再修。
3. 文本输出新增 `MAX_TOKENS` 检测与自动续写拼接；如果多轮续写后仍未完成，会显式报错，而不是静默交付半截稿。
4. 长文 mock 回放已验证 `parsedText.Length = 27010` 且任务成功结束，说明旧的硬上限已移除。
5. 当前没有新的明确提升项，因此暂不进入 `Round 8`。
